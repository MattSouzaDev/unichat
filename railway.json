{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE"
  },
  "deploy": {
    "preDeployCommand": "python manage.py migrate --noinput",
    "startCommand": "daphne -b 0.0.0.0 -p $PORT unichat.asgi:application",
    "restartPolicyType": "ON_FAILURE"
  }
}
